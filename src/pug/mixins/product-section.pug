include ./news-card

mixin productSection(array)
  +b.product-cards 
    +b.g-container--fit_sm-sm.--fit_md-md.--fit_lg-lg.--fit_xl-xl
      +b.product-cards-wrapper 
        +e.row
          each card in array
            +e.cols
              +productsCard(card)